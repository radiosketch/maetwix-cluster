apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: matrix

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - ingress.yaml

labels:
  - pairs:
      app: synapse
      app.kubernetes.io/name: synapse

images:
  - name: matrixdotorg/synapse
    newTag: v1.98.0
