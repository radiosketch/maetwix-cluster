apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: infrastructure

resources:
  - statefulset.yaml
  - service.yaml

commonLabels:
  app: etcd
  app.kubernetes.io/name: etcd
  app.kubernetes.io/component: coordination
  app.kubernetes.io/part-of: maetwix-cluster
